export const API_URL = 'http://localhost:3090';
export const PRELOADER_DELAY = 300;
export const toastrOptions = {
    timeOut: 3000, // by setting to 0 it will prevent the auto close
    position: 'bottom-left'
};
